/** Max questions per quiz for non-VIP users */
export const MAX_QUESTIONS_FREE = 20;
/** Max questions per quiz for VIP users */
export const MAX_QUESTIONS_VIP = 200;

/** Max uploaded documents (count) for free users */
export const MAX_DOCUMENTS_FREE = 10;
/** Max uploaded documents for VIP users */
export const MAX_DOCUMENTS_VIP = 100;

/** Total document storage cap for free users (bytes) */
export const MAX_TOTAL_STORAGE_BYTES_FREE = 50 * 1024 * 1024;
/** Total document storage cap for VIP users (bytes) */
export const MAX_TOTAL_STORAGE_BYTES_VIP = 500 * 1024 * 1024;
